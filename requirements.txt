# Pinned exact, not a floor. This UI's CSS reaches into Streamlit's internal
# DOM by data-testid (e.g. the sidebar reopen button, stExpandSidebarButton),
# and those names have already changed between minor releases. A floating
# `>=` constraint lets the deploy target install a newer version whose DOM
# doesn't match this CSS at all — exactly the failure mode that broke the
# sidebar on Streamlit Cloud. Bump this deliberately, verify the CSS against
# the new version's DOM, then bump the pin — never let it float.
streamlit==1.61.1
langgraph>=1.0.0
langchain-google-genai>=2.0
langchain-openai>=0.3.0
langchain-tavily>=0.1.0
markdown>=3.5
openai>=1.0
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
requests>=2.31.0
google-genai>=1.0
Pillow>=10.0
